# Archived legacy code

`mental_wellness_legacy_flask_twilio.py` is the original **Flask + Twilio** webhook bot.

The active stack is **FastAPI + Meta WhatsApp Cloud API** (`app.py`, `bot_router.py`).

This file is kept for reference only and is not installed or run in production.
