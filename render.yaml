# Render Blueprint (free-friendly): web service only, ephemeral SQLite.
# Daily backups: set ENABLE_SCHEDULED_BACKUP=true on the web service.
# For shared disk + cron job, use render.with-disk.yaml instead.
#
# FREE TIER: service sleeps after ~15 min idle. Use UptimeRobot (free) to GET
# /health every 10 min, or upgrade to starter. See DEPLOY.md → "Bot stops replying".

services:
  - type: web
    name: mental-wellness-bot
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
    envVars:
      - key: PYTHON_VERSION
        value: "3.11.9"
      - key: ENABLE_SCHEDULED_BACKUP
        value: "true"
      - key: BACKUP_KEEP
        value: "7"
      - key: ENABLE_MEDITATION_NUDGES
        value: "true"
      - key: ENABLE_DAILY_CHECKIN_NUDGES
        value: "true"
      - key: DAILY_NUDGE_HOUR
        value: "9"
      - key: DAILY_NUDGE_WINDOW_MINUTES
        value: "120"
      - key: CARE_PING_HOUR
        value: "15"
      - key: TIMEZONE
        value: "Asia/Kolkata"
